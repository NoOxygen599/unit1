class Mover {
  //instance variables
  fload x, y;    //location
  float vs, vy;  //velocity
  floatd d;      //diameter
  
  
  Mover() {
    d  = 100;
    x  = random(0, width);
    y  = random(0, height);
    vx = random(-1, 1);
    vy = random(-1, 1);
  }
  
  //       ACT           
  
  
  
  
  
  
